Git DAG Diagram
===============

The diagram file git_dag_diagram.svg contains the Git DAG visualization.

To convert SVG to PNG/JPEG (as required by the assignment), you can:
1. Open the SVG file in a web browser and take a screenshot
2. Use an online converter (e.g., convertio.co, cloudconvert.com)
3. Use image editing software (GIMP, Photoshop, etc.)
4. Use command-line tools if available (inkscape, ImageMagick, etc.)

The diagram shows:
- All commits from the last PR merge (fda6a47) to the root commit (796eb0e)
- All branch references (local and remote)
- Commit hashes (7+ digits)
- Parent-child relationships with arrows
